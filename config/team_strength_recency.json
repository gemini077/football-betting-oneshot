{
  "contract_version": "team_strength_recency.v1",
  "current_max_history_age_days": 60,
  "offseason_bridge_max_history_age_days": 180,
  "bridge_requires_verified_evidence": true,
  "notes": [
    "History recency is measured from target kickoff to the latest eligible historical match kickoff.",
    "Capture time never makes old team history current.",
    "A previous-season bridge is reported separately and is never current_strength_ready."
  ]
}
